# 全職業技能循環：路線圖與子專案 1「客戶端資料工具」

基準：`api13-tw` 於 `86ce05d`（職業介面與自動偵測已合併，使用者 2026-09-13 遊戲內實測通過）。

## 使用者決策（2026-09-13）

- 只做 90 級（絕歐米茄）；70 級（絕神兵）之後另開。
- 職業順序：暗黑騎士 → 騎士 → 白魔道士 → 賢者 → 武士 → 奪魂者 → 機工士 → 黑魔道士；其餘 12 個職業之後再排。職業名稱一律採客戶端 ClassJob 官方名稱。
- 連段與技能替換（例如基礎 1-2-3 連段、特性升級後的技能）必須做出來。
- 補血、護盾、減傷技能可施放：照常消耗、進冷卻、增減量譜、播放動畫、掛狀態圖示，但不計算數值。沿用範圍更正：不做傷害、屬性、治療、護盾、減傷的數值結算。
- 資料以本機繁中客戶端為準，並用 xivapi（https://v2.xivapi.com/）對應版本交叉核對。
- 整體做法：資料工具先做；共用機制隨職業需要才加，不預先做完。每個職業各自走「規格 → 計畫 → 實作 → 使用者實測」，實測通過才做下一個。

## 路線圖

| 子專案 | 內容 | 隨之加入的共用機制（預估，實際以該職業資料為準） |
| --- | --- | --- |
| 1 | 客戶端資料工具（本規格） | 無 |
| 2 | 暗黑騎士 | MP 消耗與回復、對隊友施放的狀態 |
| 3 | 騎士 | 玩家讀條、移動打斷讀條 |
| 4 | 白魔道士 | 目標身上的持續傷害狀態、定時增加的量譜、補血目標 |
| 5–9 | 賢者、武士、奪魂者、機工士、黑魔道士 | 只補前面沒有的（例如黑魔道士的隨機觸發、放置型技能） |

已安裝的 API 13 `FFXIVClientStructs.dll` 內含上述 8 個職業的量譜結構（DarkKnightGauge、PaladinGauge、WhiteMageGauge、SageGauge、SamuraiGauge、ReaperGauge、MachinistGauge、BlackMageGauge）。

## 子專案 1 目標與範圍

做一支工具，讀本機繁中客戶端資料，為指定職業產出 90 級的技能資料清單，並與 xivapi 對應版本交叉核對。之後每個職業子專案都以這份清單寫規則。

不包含：任何職業規則、插件程式變更、發布、推送。

## 執行方式

- 新增 `tools/ClientData/`：獨立 C# 主控台程式（net9.0），不編進插件、不新增套件。直接引用 `C:/Users/Knuckles/AppData/Roaming/FFXIVSimpleLauncher/Dalamud/Injector/` 的 `Lumina.dll` 與 `Lumina.Excel.dll`（與插件建置同一個 API 13 路徑）。
- 用法：`dotnet run --project tools/ClientData -- <職業縮寫>`；可選參數：客戶端路徑（預設 `Z:/FINAL FANTASY XIV TC/game`）、等級（預設 90）、xivapi 版本、戰士標準答案檢查模式。
- 語言固定繁體中文，開啟資料表校驗碼檢查（不符即失敗）。客戶端版本讀自 `game/ffxivgame.ver`（目前 `2026.07.22.0000.0000`），寫入清單開頭。
- 啟動器的 `Lumina.dll` 組件版本為 0.0.0.0，而 `Lumina.Excel.dll` 參照 Lumina 6.0.0.0；工具在使用任何資料表型別前註冊組件載入處理，回傳該 `Lumina.dll`。

## 資料怎麼挑

- **技能**：非 PvP、習得等級 1–90，且（技能的職業欄位是本職業或其前置職業，或屬於職能技能且職業分類含本職業）；再加上技能替換表（ActionIndirection）中本職業、習得等級 ≤ 90 的替換技能。連段前置取自技能的 ActionCombo 欄位。ActionComboRoute 經本機讀取確認是 PvP 連段路線，不使用。以戰士驗證：已知 32 個技能全數命中，多出的只有被特性升級取代的基礎技能（38、49、51、3551）。
- **特性**：該職業、習得等級 ≤ 90 的所有特性。
- **狀態**：技能與特性說明文字引用到的狀態。同名多列（例如 PvE／PvP）全部列出並標為「需人工確認」，不自動擇一。
- **說明文字**：Lumina 沒有巨集求值器。本機全資料的說明巨集只有 `colortype`、`edgecolortype`、`br` 與 `if`，條件只有 `gnum68`（職業，`==`／`>=`）、`gnum72`（等級，`>=`）與少量 `gnum69>=`。工具以本職業 ID 與 90 級求值 `gnum68`／`gnum72` 條件、去除顏色標記、`br` 轉換行；其他條件或未知標記保留原文並列入需人工確認。
- **替換關係**：兩個來源。(1) 技能替換表的狀態替換（例如裂石飛環 → 狂魂）；(2) 特性說明中「A變為B」且 A、B 皆為本職業技能名稱者（例如「原初之魂變為裂石飛環」），標註來源為特性說明，並一律列入需人工確認。

## 輸出

每個職業一份 `docs/client-data/<職業縮寫>-90.md`，依序：

1. 客戶端版本、讀取時間、對應的 xivapi 國際服版本（或「未完成 xivapi 核對」）
2. 技能表：ID、名稱、類型、習得等級、詠唱時間、冷卻時間、冷卻組、額外冷卻組、充能數、消耗類型與數值、射程、效果範圍、可選目標（自身／隊友／敵人）、連段前置、替換關係。本規格所稱「冷卻組」「額外冷卻組」一律指 Action 資料表欄位值，不等於遊戲原生冷卻陣列索引（原生索引須以 `GetRecastGroup` 取得）
3. 特性表：ID、名稱、習得等級、說明（求值後）
4. 狀態表：ID、名稱、說明、是否需人工確認
5. 每個技能的完整說明（求值後）
6. 需人工確認清單
7. 與 xivapi 的差異表

`docs/` 在 `.gitignore` 內，清單以 `git add -f` 提交。

## xivapi 交叉核對

- 只比數值與 ID 欄位，不比文字（xivapi 只有日、英、德、法文）。
- 查詢方式已驗證：`https://v2.xivapi.com/api/sheet/Action?version=7.3&rows=31,37,42&fields=...` 支援指定版本與多列批次；不存在的版本回傳 400。
- 版本選擇：預設對 7.3、7.31、7.35、7.38、7.4 各比一次並列出各版本差異數，差異最少者記為對應國際服版本，列出該版本逐欄差異；可用參數指定單一版本。
- 以多列批次查詢減少請求數。

## 錯誤處理

- 找不到客戶端路徑、資料表校驗碼不符、職業縮寫不存在（列出可用縮寫）、該職業在 90 級查無技能：結束並說明原因，不產出清單，結束代碼非 0。
- 清單先寫暫存檔，完整後才取代正式檔；中途失敗不留半份清單。
- xivapi 連線或查詢失敗：仍產出清單，開頭標示「未完成 xivapi 核對」，並以與成功不同的結束代碼結束。此清單不得直接用於撰寫規則。
- 「需人工確認」項目不是錯誤，但必定列出。
- 結束代碼：0 清單已產出且完成 xivapi 核對；1 致命錯誤、未產出；2 已產出但未完成 xivapi 核對（含使用 `--skip-xivapi`）；3 戰士標準答案檢查失敗。

## 驗證

**戰士標準答案檢查**：以 `2026-09-13-warrior-client-contract.md` 已人工核對的值為準，工具產出 WAR-90 清單後逐項比對，任一不符即失敗：

- 武器技 31、37、42、45、41、16462、3549、3550、16465、16463、25753 冷卻組 58
- 戰嚎 52：冷卻 60 秒、冷卻組 20、額外冷卻組 71、充能 2
- 猛攻 7386：冷卻 30 秒、冷卻組 8、額外冷卻組 72、資料列充能 2；特性 421 列出（88 級起充能 3）
- 動亂 7387 與群山隆起 25752 同冷卻組
- 自身範圍技 41、16462、3550、16463、25752 效果範圍 5；46、7386、25753 射程 20
- 狀態 1177 與 1303 同名，兩列都列出且標為需人工確認；1897、2677、2624 列出
- 升級 38 → 7389、49 → 3549、51 → 3550、3551 → 25751 出現在替換關係或需人工確認清單
- 特性 157、267、421、505 列出

**離線檢查**：xivapi 差異計算與版本挑選邏輯，以固定假資料驗證，不需連網。

既有插件測試（`Tests/AnoMech.P3.Tests.csproj`、`Tests.PartyLayout`）照舊全部通過。

## 驗收

1. 戰士標準答案檢查通過。
2. 為暗黑騎士產出 `docs/client-data/DRK-90.md`，完成 xivapi 核對並記錄對應國際服版本。
3. 使用者大致瀏覽暗黑騎士清單，確認技能、特性、狀態沒有明顯缺漏；逐項核對留待子專案 2。
4. 只新增工具與清單，不改插件程式，不發布、不推送。
